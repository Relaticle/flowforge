public function editAction(Action $action): Action
{
    return $action
        ->modalHeading('Edit {{ model }}')
        ->modalWidth('md')
        ->form(function (Forms\Form $form) {
            return $form->schema([
                {{ editFields }}
            ]);
        });
} 